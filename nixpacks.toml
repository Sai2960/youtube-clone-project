[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm ci --prefer-offline=false --cache=/tmp/.npm-cache']

[phases.build]
cmds = []

[start]
cmd = 'node index.js'

[variables]
NODE_ENV = 'production